{
  "default": {
    "parallel": 2,
    "publishQuiet": true,
    "tags": "@qa_ready",
    "requireModule": ["ts-node/register"],
    "require": ["support/**/*.ts"],
    "format": ["html:cucumber-report.html"]
  }
}
