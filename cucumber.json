{
  "default": {
    "parallel": 2,
    "publishQuiet": true,
    "tags": "not @todo",
    "requireModule": ["ts-node/register"],
    "require": ["support/**/*.ts"],
    "format": ["html:cucumber-report.html"]
  }
}
